# 更新记录

版本号用[语义化版本](https://semver.org/lang/zh-CN/)：
`主版本.次版本.修订号`。改了什么就往上加一条，发版时把当天的日期补上。

## [1.0.3] — 2026-09-19

三个窗口的缩放一并调整，重点是把「缩不动」和「挤在一起」这两件事分开处理。

**新增**

- **缩放角标**：主窗口、设置、回收站右下角各有一个。窗口边框本身即可拖动
  缩放，但右下角那条判定带仅数像素宽，不易命中；角标将它扩大至 16×16。
  窗口最大化时自动隐藏。
- **按钮栏自动收纳**：主窗口与回收站的底部按钮栏，宽度不足时按「由右至左」
  的顺序收入「▾」下拉菜单，不再相互挤压。收纳的按钮在下拉菜单与右键菜单中
  均可访问。窗口最小宽度相应改为依据按钮的实际尺寸计算 —— 此前它被「整排
  按钮都展开」的宽度顶死，缩到那儿就到头了。
- **设置对话框可缩放**：此前其最小尺寸由内部控件的尺寸决定，几乎缩不动。
  现改为内容保持 480 像素的底线，窗口可缩至 280×220，再小则由自动出现的
  横纵滚动条接管，不再把控件压扁。打开尺寸不变。

**改动**

- 设置与回收站右下角的「关闭」按钮移除。标题栏的关闭按钮与 Esc 均可关闭
  对话框，该按钮所占的位置改放缩放角标。

**修复**

- 窗口尺寸的记忆。此前将窗口缩至最小后关闭，再次打开时会被撑回「按钮栏
  全部展开」的宽度。原因是次序问题：窗口显示时 Qt 以布局的最小宽度为底线，
  而按钮收纳在显示之后才执行；现将最小宽度提前至显示之前设置。


## [1.0.2] — 2026-09-19

**修复**

- 迁移工作区之后，对话记录里的路径由反斜杠变成了正斜杠（`D:\…` 写成 `D:/…`）。
  目录选择框返回的路径用正斜杠，写入前未作归一化。Windows 对两种写法都认，
  但与其余记录不一致。现统一归一化为反斜杠。
- 迁移确认框的措辞可能被理解为「旧工作区里的工作文件也会一并搬走」。
  现明确说明只移动对话文件，用户自己放在旧工作区里的文件不受影响。


## [1.0.1] — 2026-09-19

**修复**

- 「打开目录」改为直接调用 `explorer.exe`。
  原实现依赖系统「打开文件夹」的关联动作。该关联被系统设置或第三方工具
  修改后，可能启动终端而非文件管理器，也可能不产生任何结果。
  新实现不经过该关联。
- 修正操作与提示的先后顺序。原实现先更新状态栏、再执行操作，
  因而操作失败时状态栏仍显示「已在新窗口打开」。现改为执行成功后再提示，
  失败时给出明确说明。

**新增**

- 主列表支持右键菜单，操作项与按钮栏一致：
  接着聊、改名、复制命令、打开目录、迁移工作区、删除。
- 回收站列表支持右键菜单：恢复、彻底删除、清空回收站。
- 回收站列表支持点击表头排序，可按名称、原文件夹、删除时间排列，
  默认按删除时间降序。


## [1.0.0] — 2026-09-17

第一个版本。

**浏览**

- 把本机所有项目的 Claude Code 对话列进一张表，不再局限于当前项目的 `/resume`
- 按名字 / 文件夹 / 最后提问搜索，按任意列排序，列宽可拖并记住

**整理**

- 双击一行，在终端里接着这个对话聊
- `F2` 改名 —— 新名字在 Claude Code 自己的 `/resume` 里也会显示
- `Delete` 删除，进回收站；可恢复，也可彻底删除或清空
- 迁移工作区：改写对话里记录的路径并把文件搬到对应项目文件夹，原文件先备份

**新建**

- 在默认工作区里开新对话
- 可选「每次对话使用独立子目录」：按时间戳建子目录，并要求 Claude 把本次产出都放进去

**外观**

- 7 套主题：4 套浅色、3 套深色，深浅两套界面设置各自独立保存
- 背景图，带淡化和柔化；面板不透明度、文字对比、隔行深浅交替可调

**其他**

- 中英文界面，可随时切换
- 免安装便携版：不写注册表、不留后台，删文件夹就是卸载
- 设置存在 `%APPDATA%\claude-session-list`，删程序不会删它
