# exe 的文件属性（右键「属性 → 详细信息」里那些）。
# 编码必须是 UTF-8 —— 里面有中文。
#
# StringTable 的第一个参数 '080404B0'：
#   0804 = 简体中文，04B0 = Unicode。跟下面 VarFileInfo 的 [2052, 1200] 对应
#   （2052 是十进制的中文 LCID，1200 是 Unicode 代码页）。

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 2, 0),
    prodvers=(1, 0, 2, 0),
  ),
  kids=[
    StringFileInfo([
      StringTable(
        '080404B0',
        [StringStruct('CompanyName', ''),
         StringStruct('FileDescription', '会话簿 — Claude Code 对话管理器'),
         StringStruct('FileVersion', '1.0.2.0'),
         StringStruct('ProductName', 'SessionLedger'),
         StringStruct('ProductVersion', '1.0.2.0')])
    ]),
    VarFileInfo([VarStruct('Translation', [2052, 1200])])
  ]
)
